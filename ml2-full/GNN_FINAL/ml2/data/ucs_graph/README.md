# Data Duplication Notice

**Post-Merge Cleanup Flag:** The data in this directory is a **duplicate** of the canonical data produced by the data-engineering pipeline. 

Once the monorepo merge is finalized, the code in this track should be updated to point to the shared canonical copy to avoid carrying redundant multi-megabyte data files. 

For now, this local copy is retained to preserve the working paths of the verified `z(t)` encoder and ablation scripts without destabilizing them. **Do not treat this copy as an independent source of truth.**
