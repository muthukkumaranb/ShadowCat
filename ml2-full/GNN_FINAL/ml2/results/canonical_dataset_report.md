# Canonical Dataset Recovery Report

Generated by `python -m ml2.experiments.sanity_checks` on 2026-09-08.

## Dataset identity

- Source: canonical UCS artifacts from `SIH2026---UCS-INGESTION-PIPELINE`.
- Canonical windows: 2,787 post-purge windows.
- Split counts: train 2,013, validation 369, test 405.
- Date range: `2018-02-14 01:00:00+00:00` to `2018-03-02 12:59:00+00:00`.
- Canonical graph node IDs are all present in `node_lookup.parquet`.

## Slot-10 sanity check

### Coverage history

The original LOEO export covered **233/2787** canonical windows and had
**0% validation coverage**. The replacement chronological export covers
**2275/2787** windows overall, including **837/837 validation+test windows
(100%)**. The remaining train gap is the first 512 walk-forward windows,
which have no prior history for ML1's exporter.

### Chronological export result

```text
Artifact rows: 2275
Artifact unique windows: 2275
Artifact exact canonical-window matches: 2275
full_graph_sequence train: rows=1501, unique_windows=1501, slot10_fill_rate=0.745653
full_graph_sequence val: rows=369, unique_windows=369, slot10_fill_rate=1.000000
full_graph_sequence test: rows=405, unique_windows=405, slot10_fill_rate=1.000000
```

The chronological CSV uses `window_start_utc` and
`normalized_deviation_score` (log1p plus robust-normalized); the ML2 adapter
now accepts these names and still applies the required one-window causal shift.

### Prior LOEO result retained for comparison

Literal artifact result:

```text
Artifact rows: 412
Artifact unique windows: 233
Artifact zero deviation rows: 0
Artifact exact canonical-window matches: 412
full_graph_sequence train: rows=294, unique_windows=161, slot10_fill_rate=0.079980
full_graph_sequence val: rows=0, unique_windows=0, slot10_fill_rate=0.000000
full_graph_sequence test: rows=118, unique_windows=72, slot10_fill_rate=0.177778
matched_deviation_population train: rows=296, unique_windows=163, slot10_fill_rate=1.000000
matched_deviation_population val: rows=45, unique_windows=34, slot10_fill_rate=1.000000
matched_deviation_population test: rows=71, unique_windows=36, slot10_fill_rate=1.000000
```

**Blocking finding:** the full canonical split is mostly zero-filled for slot 10,
including 100% unfilled validation windows and 82.22% unfilled test windows. The
real ablation must not run until ML1 supplies a complete canonical-window
deviation export, or the experiment explicitly uses and reports the matched
233-window population.

The matched population has nonzero scores in every split, but it is not the full
2,787-window canonical ablation population and therefore cannot silently replace it.

## Normalization input

The prior LOEO deviation artifact had `min=14.1623`, `median=67.7217`, and
`max=20589`; its `log1p` median was `4.23007` and MAD `0.0357144`.
The chronological artifact is already normalized, so its values must not be
passed through `log1p` again. The current sanity check reports its normalized
distribution directly.

The chronological export passes the coverage gate, but the actual 64-dimensional
ML1 temporal latent `z(t)` is not present in ML2 or in the published ML1
artifacts. Therefore the dynamics ablation remains blocked rather than being
run with the existing zero-latent placeholder.